# Making the Grade

Focus:
- Loops
- Lists

<br>

Comments:
- Using an underscore instead of i or x in for loop statements will prevent unused variables
- .extend() can be used instead of .append(), which unlike .append(), is able to add multiple individual items from an iterable.
- .enumerate() can be used to combine two lists
