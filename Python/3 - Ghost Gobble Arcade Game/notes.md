# Ghost Gobble Arcade Game

Focus:
- Booleans

<br>

Comments:
- Using return bool(test) is much cleaner and faster to type out
- When using if statements, no need to elif after a return since return will exit the function immediately
